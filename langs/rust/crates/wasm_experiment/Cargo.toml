[package]
name = "wasm_experiment"
version = "0.1.0"
edition = "2024"

[dependencies]
ufmt = "0.2.0"

[profile.dev]
panic = "abort"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"

